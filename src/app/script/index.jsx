import React from 'react';

const app = React.createClass({
  render: () => {
    return ( // eslint-disable-line no-extra-parens
      <h1>
        Hi ~ <small>yaya</small>
      </h1>
    );
  },
});

export default app;
