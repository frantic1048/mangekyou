import React from 'react';

const FilePanelCard = React.createClass({
  render() {
    return ( // eslint-disable-line no-extra-parens
      <p style={{display: 'inline-block', width: `${1 / 5}%`}}>A FilePanelCard</p>
    );
  },
});

export default FilePanelCard;
