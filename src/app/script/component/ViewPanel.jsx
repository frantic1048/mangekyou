import React from 'react';

const ViewPanel = React.createClass({
  render() {
    return ( // eslint-disable-line no-extra-parens
      <p>View Panel Body</p>
    );
  },
});

export default ViewPanel;
